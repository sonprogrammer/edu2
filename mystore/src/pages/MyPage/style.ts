import styled from "styled-components";
import tw from "twin.macro";

export const StyledContainer = styled.div`
    ${tw`
        
    `}
`
export const StyledContaine = styled.div`
    ${tw`

    `}
`
export const StyledText = styled.div`
    ${tw`
        h-full
        flex
        justify-center
        items-center
        text-[33px]
        font-bold
        text-purple-400
    `}
`